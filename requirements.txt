# =========================================================================
# Runtime
# =========================================================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0

# =========================================================================
# DB
# =========================================================================
sqlalchemy>=2.0.25
alembic>=1.13.1
psycopg[binary,pool]>=3.1.18
aiosqlite>=0.20.0

# =========================================================================
# Validation / Settings
# =========================================================================
pydantic>=2.6.0
pydantic-settings>=2.1.0
email-validator>=2.1.0.post1
python-multipart>=0.0.9

# =========================================================================
# Auth & Security
# =========================================================================
# python-jose >=3.4.0 corrige CVE-2024-33663 (algorithm confusion) y
# CVE-2024-33664 (JWT/JWE bomb DoS). NO bajar de 3.4.0.
python-jose[cryptography]>=3.4.0
passlib[argon2,bcrypt]>=1.7.4
bcrypt==4.0.1
cryptography>=43.0.0
slowapi>=0.1.9
redis>=5.0.0
limits[redis]>=3.13.0
aiosmtplib>=3.0.0
jinja2>=3.1.0

# =========================================================================
# Observability / Logging
# =========================================================================
structlog>=24.1.0

# =========================================================================
# Documentos (actas de entrega en Word .docx y PDF, con logo de empresa)
# =========================================================================
python-docx>=0.8.11
reportlab>=4.1.0
pillow>=10.3.0
httpx>=0.27.0  # descarga del logo configurado (runtime, no solo tests)

# =========================================================================
# 2FA / MFA (autenticación de dos factores)
# =========================================================================
pyotp>=2.9.0        # TOTP (RFC 6238) para apps autenticadoras
qrcode>=7.4.2       # QR de enrolamiento TOTP (usa pillow, ya presente)

# =========================================================================
# Testing (dev)
# =========================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# Auditoría de vulnerabilidades de dependencias (correr en CI):
#   pip-audit -r requirements.txt
pip-audit>=2.7.0
